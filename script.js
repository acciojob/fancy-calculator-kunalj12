//your JS code here. If required.
 let firstVariable = prompt("Enter the first variable:");
      let result = (firstVariable * 2) + Number(prompt("Enter the second variable:"));
      alert("The final result is: " + result);